import type { Metadata } from "next";import Link from "next/link";import { editions } from "../../lib/content";
export const metadata:Metadata={title:"Ediciones",description:"El archivo completo de la revista Órbita."};
export default function EditionsPage(){return <div className="page-shell listing-page"><div className="listing-intro"><span className="eyebrow">ARCHIVO DE ÓRBITA</span><h1>Cada número es una constelación.</h1><p>Explora las ediciones completas o abre sus historias como artículos independientes.</p></div><div className="edition-archive">{editions.map((e)=><Link href={`/ediciones/${e.slug}`} key={e.slug}><div className={`cover cover-${e.color}`}><span>ÓRBITA</span><strong>{e.number}</strong><div className="cover-orbit"></div><h2>{e.title}</h2><small>REVISTA DE DIVULGACIÓN CIENTÍFICA</small></div><div className="edition-label"><span>{e.year} · NÚMERO {e.number}</span><h2>{e.title}</h2><p>{e.summary}</p><b>Explorar edición →</b></div></Link>)}</div></div>}

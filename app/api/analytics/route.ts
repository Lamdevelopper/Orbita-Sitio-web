import { getDb } from "../../../db";
import { audienceEvents } from "../../../db/schema";
import { cleanText, routeError } from "../../../lib/api";
const allowed=new Set(["page_viewed","active_read_30_seconds","article_25_percent","article_50_percent","article_75_percent","article_90_percent","share_clicked","link_copied","related_article_clicked","edition_opened","pdf_opened","pdf_downloaded","search_used"]);
export async function POST(request:Request){try{if(request.headers.get("cookie")?.includes("orbita_consent=all")!==true)return new Response(null,{status:204});const body=await request.json() as Record<string,unknown>;const eventName=cleanText(body.eventName,60);if(!allowed.has(eventName))return Response.json({error:"Evento no permitido"},{status:400});let referrerHost:string|null=null;try{referrerHost=body.referrer?new URL(String(body.referrer)).hostname:null}catch{}const properties=body.properties&&typeof body.properties==="object"?body.properties as Record<string,unknown>:{};if(JSON.stringify(properties).length>4000)return Response.json({error:"Propiedades demasiado grandes"},{status:413});await getDb().insert(audienceEvents).values({eventName,anonymousId:cleanText(body.anonymousId,80),sessionId:cleanText(body.sessionId,80),articleSlug:cleanText(body.articleSlug,180)||null,path:cleanText(body.path,500),referrerHost,properties,occurredAt:new Date()});return new Response(null,{status:204})}catch(error){return routeError(error)}}
